import LockerRentalController from './LockerRentalController'
import Settings from './Settings'

const Controllers = {
    LockerRentalController: Object.assign(LockerRentalController, LockerRentalController),
    Settings: Object.assign(Settings, Settings),
}

export default Controllers